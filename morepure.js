const Aluno = (studiesone , studiestwo) => ( studiesone + studiestwo)
console.log(Aluno('martins struggling' , ' beatriz'))

const shoot = (one, two) => (one / two)
console.log(shoot(122,6))

const Caught = (four, five) => (four ** five)
const Giveup = Caught(10,5)
console.log(Giveup)


const phase = ` hi there i am together another number ${Giveup}
`
console.log(phase)
